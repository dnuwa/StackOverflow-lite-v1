tags:
- "Subscribers"
summary: "Add a new subscriber"
description: "This is a POST request to add a new user"

consumes:
- "application/json"

produces:
- "application/json"

parameters:
- in: "body"
  name: "body"
  description: "user details to be added to the list"
  required: true
  schema:
    $ref: "#/definitions/Subscriber"
responses:
  405:
    description: "Invalid input"

  201:
    description: "Subscriber added successfuly"

definitions:
  Subscriber:
    type: "object"
    properties:
      
      display_name:
        type: "string"

      email:
        type: "string"

      password:
        type: "string"